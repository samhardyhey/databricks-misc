# The main pipeline for healthcare_data
resources:
  pipelines:
    healthcare_data_pipeline:
      name: healthcare_data_pipeline
      catalog: workspace
      schema: healthcare_data_${bundle.target}
      serverless: true
      libraries:
        - notebook:
            path: ../src/pipeline.ipynb

      configuration:
        bundle.sourcePath: ${workspace.file_path}/src
